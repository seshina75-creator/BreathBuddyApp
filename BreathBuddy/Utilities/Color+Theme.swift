import SwiftUI

extension Color {
    static let appBackground = Color(.systemGroupedBackground)
    static let appCard = Color(.secondarySystemGroupedBackground)
    static let appAccent = Color(red: 0.36, green: 0.65, blue: 0.78)
}
